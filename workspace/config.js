// config.js
const config = {
    app: {
        port: 80
    },
    db: {
        host: 'localhost',
        user: 'postgres',
        password:'',
        dbname: 'postgres'
    }
};

module.exports = config;
