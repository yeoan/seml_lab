// config.js
const config = {
    app: {
        port: 8080
    },
    db: {
        host: 'localhost',
        user: 'ubuntu',
        password: 'ubuntu',
        dbname: 'ubuntu'
    }
};

module.exports = config;
